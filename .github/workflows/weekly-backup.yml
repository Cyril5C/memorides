name: Weekly Backup

on:
  schedule:
    # Tous les dimanches à 3h du matin (UTC)
    - cron: '0 3 * * 0'
  # Permet de lancer manuellement le backup
  workflow_dispatch:

jobs:
  backup:
    runs-on: ubuntu-latest
    container: ghcr.io/railwayapp/cli:latest
    env:
      RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Node.js in container
        run: |
          apk add --no-cache nodejs npm

      - name: Install dependencies (without postinstall)
        run: npm install --ignore-scripts

      - name: Link Railway project
        run: |
          echo "🔗 Linking to Railway project..."
          railway link --project 024458df-b6d3-4a4b-a537-bcb583c4502a --environment production

      - name: Run backup via Railway
        run: |
          echo "🚀 Starting complete backup from Railway production..."
          echo "📊 Exporting database, GPX files, and photos..."
          railway run --service memorides node export-data.js

      - name: Get backup date
        id: date
        run: echo "date=$(date +'%Y-%m-%d')" >> $GITHUB_OUTPUT

      - name: Upload backup artifacts
        uses: actions/upload-artifact@v4
        with:
          name: memorides-backup-${{ steps.date.outputs.date }}
          path: exports/memorides-backup-*
          retention-days: 90

      - name: Send notification on failure
        if: failure()
        run: |
          echo "❌ Backup failed!"
          # Vous pouvez ajouter une notification (email, Slack, etc.)
